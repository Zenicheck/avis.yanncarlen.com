{% extends 'base.html.twig' %}

{% block title %}RateAccess{% endblock %}

{% block body %}
    <h1>RateAccess</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ rate_access.id }}</td>
            </tr>
            <tr>
                <th>Uniq</th>
                <td>{{ rate_access.uniq }}</td>
            </tr>
            <tr>
                <th>Open</th>
                <td>{{ rate_access.open ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('rate_access_index') }}">back to list</a>

    <a href="{{ path('rate_access_edit', {'id': rate_access.id}) }}">edit</a>

    {{ include('rate_access/_delete_form.html.twig') }}
{% endblock %}
